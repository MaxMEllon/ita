const { h, Text, Component } = require('ink');

class Dashboard extends Component {
  constructor(props) {
    super(props);
  }

  render() {
    return (
      <div>
        <span>Dashboard</span>
      </div>
    )
  }
}

module.exports = Dashboard;
